export interface ILayoutOption {
    headerWidth: string;
    smHeaderWidth: string;
    sidebarWidth: string;
}

const layout: ILayoutOption = {
    headerWidth: '96px',
    smHeaderWidth: '56px',
    sidebarWidth: '330px',
};

export default layout;
